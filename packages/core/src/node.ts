/** Nur für Node (Skripte, Monitor, API). Nicht im Browser-Bundle verwenden. */
export * from './watchlist/json-file-store';

/** PDF-Text lesen (unpdf): nur für Node, nicht im Browser-Bundle. */
export * from './adapters/news/kap-document';
